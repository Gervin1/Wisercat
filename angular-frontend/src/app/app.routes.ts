import { Routes } from '@angular/router';
import { FiltersListComponent } from "./filters-list/filters-list.component";

export const routes: Routes = [
  { path: '', component: FiltersListComponent },
];
